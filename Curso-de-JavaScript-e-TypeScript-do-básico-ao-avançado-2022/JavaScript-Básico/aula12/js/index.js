const primeiroNumero = prompt('Digite um número: ')
const segundoNumero = prompt('Digite outro número: ')
const resultadoSoma = Number(primeiroNumero) + Number(segundoNumero)

alert(`${primeiroNumero} + ${segundoNumero} = ${resultadoSoma}`)